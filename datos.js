// Este archivo pertence a la capa de datos pues solo recibe y devuelve datos, sin proveer
// información adicional.

// Esta función se encarga de recibir los datos que logica.js le envió, y suma ambos valores
// para sacar un resultado y devolverselo al archivo mencionado.
function obtenerResultado(a,b){
    return a + b;
}